# this has been kept just for consistency with the organization of the project
# if needed, it can be used to extend and further configure the mysql image from here
# without having to change the docker-compose.yml file


FROM mysql:8.1.0
